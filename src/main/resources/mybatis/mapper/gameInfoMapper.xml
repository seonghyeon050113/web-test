<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mvn.gameInfoMapper">
	<select id="selectGameInfos"
		resultType="com.web.dto.gameInfoDTO">
		select GI_NUM,GI_NAME,GI_PRICE,GI_GENRE,GI_DESC
		FROM GAME_INFO
		<where>
			<if test='giName != null and giName !=""'>
				AND GI_NAME LIKE CONCAT('%',#{giName},'%')
			</if>
			<if test='giPrice != null and giPrice !=0'>
				AND GI_PRICE = #{giPrice}
			</if>
			<if test='giGenre != null and giGenre !=""'>
				AND GI_GENRE = LIKE CONCAT('%',#{giGenre},'%')
			</if>
			<if test='giDesc != null and giPrice !=""'>
				AND GI_DESC = LIKE CONCAT('%',#{giDesc},'%')
			</if>
		</where>
	</select>
	<select id="selectGameInfo" resultType="com.web.dto.gameInfoDTO">
		select GI_NAME,GI_PRICE,GI_GENRE,GI_DESC
		FROM GAME_INFO 
		WHERE GI_NUM = #{giNum}
	</select>
</mapper>